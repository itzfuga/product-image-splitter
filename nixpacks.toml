[phases.setup]
nixPkgs = ["python311", "tesseract", "opencv4"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "gunicorn app:app --host 0.0.0.0 --port $PORT"